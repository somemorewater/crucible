name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  check:
    name: Minimal Workspace Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Verify repository
        run: |
          echo "Repository is checked out and minimal workspace check passed successfully!"